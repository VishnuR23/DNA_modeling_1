# @package _global_
# Local end-to-end pilot: data must first be downloaded with alanine_download.
defaults:
  - override /data: alanine
action: pilot
model:
  condition_readout: false
evaluation:
  rng_mode: checkpoint
